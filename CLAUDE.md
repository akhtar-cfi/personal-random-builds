# CLAUDE.md

## Deliverables rule (set by Akhtar via Oracle, 2026-08-02)

Every deliverable file created in a session (deck, document, sheet, report, export) must be committed and pushed to this repo before the task ends — Oracle, Akhtar's chief-of-staff agent, fetches deliverables from GitHub; unpushed work is invisible to it.

Exception — files larger than 10 MB: do NOT commit them. Upload to Google Drive under `Claude Code Files/personal-random-builds/` (create the folders if missing) and put the Drive link in the final summary. If the session has no Drive access, say so explicitly and note where the file lives.
