# Paste this as your first message in Claude Code (from the kit directory)

Read SKILL.md in this directory fully before doing anything — it is the complete
context for Crashfree India's AOP 2026-27 deck and budget workbook, including the
slide map, exact Rs 6.00 Cr budget line items, design-system rules, facts-vs-assumptions
register, and the mandatory QA loop.

Working rules:
1. Edit build_deck.py / build_budget.py and regenerate — do not hand-edit the binaries
   except for trivial text tweaks.
2. Follow the CFI design system tokens in SKILL.md Section 5 exactly (Montserrat/Geist,
   #4A35FF brand, 6.9in bottom safe line, two-line titles).
3. After ANY deck change: render every changed slide to an image and inspect it
   (SKILL.md Section 8). After ANY budget change: recalc and confirm 0 formula errors
   and a Rs 600.00 L grand total (unless I change the total).
4. Never invent statistics or sources. SKILL.md Section 6 lists which numbers are
   verified vs assumed vs conflicting — keep that register updated as we resolve items.
5. Coerce all computed shape geometry to int() (float EMUs corrupt PowerPoint files).

First task: [describe your change here]
